#
function myTemperature()
{
int low, high, step;
bool test := true;
if(test := true){
return false;
}
while (low< high)
	{
		print(low);
		write(convert[low]);
		low := low+step;
	}
return low;
}
#
#