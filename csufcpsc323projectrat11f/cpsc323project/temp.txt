int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
}int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)
	{
	print(low);
	write(convert[low]);
	low = low + step;
	}
int low, high, temp
{
	return 5*(fahr-32)/9;
}

int low, high , step;

scan(low, high, step);
while(low<high)	